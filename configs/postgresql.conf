listen_addresses = '*'
max_connections = 100
shared_buffers = 256MB

# zt-server's postgresql2 (Dart 2.x) only supports MD5. Match POSTGRES_HOST_AUTH_METHOD.
password_encryption = md5

# Faster initial chain indexing per nom-indexer README.
synchronous_commit = off
